import { useState } from "react"; import { ToolLayout, ToolInput, ToolOutput } from "./ToolComponents";
const WordCounter = () => { const [t,sT]=useState(""); const words=t.trim()?t.trim().split(/\s+/).length:0; const chars=t.length; const sentences=t.split(/[.!?]+/).filter(s=>s.trim()).length; const paragraphs=t.split(/\n\n+/).filter(s=>s.trim()).length; return <ToolLayout><ToolInput label="Text" value={t} onChange={sT} multiline rows={6} /><div className="grid grid-cols-2 sm:grid-cols-4 gap-3">{[["Words",words],["Characters",chars],["Sentences",sentences],["Paragraphs",paragraphs]].map(([l,v])=><div key={String(l)} className="glass rounded-xl p-4 text-center gradient-border"><p className="text-2xl font-bold gradient-text">{String(v)}</p><p className="text-xs text-muted-foreground">{String(l)}</p></div>)}</div></ToolLayout>; };
export default WordCounter;
