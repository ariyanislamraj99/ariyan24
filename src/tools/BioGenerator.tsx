import { useState } from "react"; import { ToolLayout, ToolInput, ToolOutput, ToolButton, ToolSelect } from "./ToolComponents";
const BioGenerator = () => { const [name,sN]=useState(""); const [role,sR]=useState(""); const [tone,sT]=useState("professional"); const [o,sO]=useState("");
  const gen=()=>{const bios:Record<string,string>={professional:`${name} | ${role} | Building innovative solutions | Let's connect and collaborate 🤝`,casual:`Hey! I'm ${name} 👋 ${role} by day, creator by night 🌙 Let's build something cool together! 🚀`,minimal:`${name} · ${role}`,creative:`✨ ${name} | ${role} | Turning ideas into reality | Coffee → Code → Create ☕💻🎨`};sO(bios[tone]||bios.professional)};
  return <ToolLayout><ToolInput label="Name" value={name} onChange={sN} placeholder="Ariyan Islam Raj" /><ToolInput label="Role" value={role} onChange={sR} placeholder="Full-Stack Developer" /><ToolSelect label="Tone" value={tone} onChange={sT} options={[{value:"professional",label:"Professional"},{value:"casual",label:"Casual"},{value:"minimal",label:"Minimal"},{value:"creative",label:"Creative"}]} /><ToolButton onClick={gen}>Generate Bio</ToolButton><ToolOutput label="Bio" value={o} /></ToolLayout>; };
export default BioGenerator;
