import { useState } from "react"; import { ToolLayout, ToolOutput, ToolButton, ToolNumber, ToolSelect } from "./ToolComponents";
const words="lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua ut enim ad minim veniam quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur excepteur sint occaecat cupidatat non proident sunt in culpa qui officia deserunt mollit anim id est laborum".split(" ");
const LoremIpsum = () => { const [count,sC]=useState(3); const [type,sT]=useState("paragraphs"); const [o,sO]=useState("");
  const gen=()=>{ if(type==="words"){ const r=[]; for(let i=0;i<count;i++) r.push(words[Math.floor(Math.random()*words.length)]); sO(r.join(" ")+"."); } else if(type==="sentences"){ const r=[]; for(let i=0;i<count;i++){ const len=8+Math.floor(Math.random()*12); const s=[]; for(let j=0;j<len;j++) s.push(words[Math.floor(Math.random()*words.length)]); s[0]=s[0][0].toUpperCase()+s[0].slice(1); r.push(s.join(" ")+"."); } sO(r.join(" ")); } else { const r=[]; for(let i=0;i<count;i++){ const sents=3+Math.floor(Math.random()*4); const p=[]; for(let j=0;j<sents;j++){ const len=8+Math.floor(Math.random()*12); const s=[]; for(let k=0;k<len;k++) s.push(words[Math.floor(Math.random()*words.length)]); s[0]=s[0][0].toUpperCase()+s[0].slice(1); p.push(s.join(" ")+"."); } r.push(p.join(" ")); } sO(r.join("\n\n")); } };
  return <ToolLayout><div className="flex gap-4"><ToolNumber label="Count" value={count} onChange={sC} min={1} max={100} /><ToolSelect label="Type" value={type} onChange={sT} options={[{value:"paragraphs",label:"Paragraphs"},{value:"sentences",label:"Sentences"},{value:"words",label:"Words"}]} /></div><ToolButton onClick={gen}>Generate</ToolButton><ToolOutput label="Lorem Ipsum" value={o} /></ToolLayout>; };
export default LoremIpsum;
