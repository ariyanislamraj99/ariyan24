import { useState } from "react"; import { ToolLayout, ToolOutput } from "./ToolComponents";
const ColorPalette = () => { const [base,sB]=useState("#6366f1"); const gen=(h:string)=>{const r=parseInt(h.slice(1,3),16),g=parseInt(h.slice(3,5),16),b=parseInt(h.slice(5,7),16);return Array.from({length:5},(_,i)=>{const f=0.6+i*0.1;return`#${Math.min(255,Math.round(r*f)).toString(16).padStart(2,"0")}${Math.min(255,Math.round(g*f)).toString(16).padStart(2,"0")}${Math.min(255,Math.round(b*f)).toString(16).padStart(2,"0")}`})};
  return <ToolLayout><div className="flex items-center gap-4"><label className="text-sm text-foreground">Base Color:</label><input type="color" value={base} onChange={e=>sB(e.target.value)} className="w-12 h-12 rounded cursor-pointer" /></div><div className="flex gap-2">{gen(base).map((c,i)=><div key={i} className="flex-1 h-20 rounded-xl border border-glass-border/20 flex items-end justify-center pb-1" style={{backgroundColor:c}}><span className="text-[10px] text-white drop-shadow">{c}</span></div>)}</div></ToolLayout>; };
export default ColorPalette;
