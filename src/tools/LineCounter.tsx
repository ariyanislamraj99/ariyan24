import { useState } from "react"; import { ToolLayout, ToolInput, ToolOutput } from "./ToolComponents";
const LineCounter = () => { const [t,sT]=useState(""); const lines=t?t.split("\n").length:0; const nonEmpty=t?t.split("\n").filter(l=>l.trim()).length:0; return <ToolLayout><ToolInput label="Text" value={t} onChange={sT} multiline rows={6} /><div className="grid grid-cols-2 gap-3"><div className="glass rounded-xl p-4 text-center gradient-border"><p className="text-2xl font-bold gradient-text">{lines}</p><p className="text-xs text-muted-foreground">Total Lines</p></div><div className="glass rounded-xl p-4 text-center gradient-border"><p className="text-2xl font-bold gradient-text">{nonEmpty}</p><p className="text-xs text-muted-foreground">Non-empty Lines</p></div></div></ToolLayout>; };
export default LineCounter;
