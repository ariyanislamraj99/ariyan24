import { useState } from "react"; import { ToolLayout, ToolInput } from "./ToolComponents";
const OgPreview = () => { const [title,sT]=useState("My Page Title"); const [desc,sD]=useState("A brief description of the page content."); const [img,sI]=useState(""); return <ToolLayout><ToolInput label="Title" value={title} onChange={sT} /><ToolInput label="Description" value={desc} onChange={sD} /><ToolInput label="Image URL" value={img} onChange={sI} placeholder="https://example.com/image.jpg" /><div className="glass rounded-xl overflow-hidden gradient-border max-w-lg"><div className="aspect-video bg-muted flex items-center justify-center">{img?<img src={img} className="w-full h-full object-cover" alt="" />:<span className="text-muted-foreground text-sm">No image</span>}</div><div className="p-4"><p className="text-xs text-muted-foreground uppercase">example.com</p><p className="text-sm font-semibold text-foreground mt-1">{title}</p><p className="text-xs text-muted-foreground mt-1 line-clamp-2">{desc}</p></div></div></ToolLayout>; };
export default OgPreview;
